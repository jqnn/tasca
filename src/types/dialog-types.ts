export type MutationMessages = {
  loading: string;
  success: string;
};

export const defaultMutationMessages: MutationMessages = {
  loading: "common.mutation.loading",
  success: "common.mutation.success",
};